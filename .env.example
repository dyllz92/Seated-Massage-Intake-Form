# Seated Massage Intake Form - Environment Configuration
# Copy this file to `.env` and fill in the values for your environment
# DO NOT commit the `.env` file with real secrets to version control

# ====== Server Configuration ======
PORT=3000
NODE_ENV=production

# ====== Google Drive Configuration ======
# Option 1: JSON content as environment variable (recommended for cloud deployment)
# Paste the entire contents of your google-credentials.json file as a single line
# GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"..."}
#
# Option 2: Path to credentials file (for local development)
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./google-credentials.json

# Google Drive folder ID where PDFs will be uploaded
GOOGLE_DRIVE_FOLDER_ID=your-folder-id-here

# Allow saving PDFs locally when Google Drive is not configured
ALLOW_LOCAL_PDF_FALLBACK=false

# ====== Analytics Dashboard - User Management ======
# Admin account credentials (required for dashboard access)
# These will be used to create the initial admin account when the server starts
ADMIN_EMAIL=admin@yourcompany.com
ADMIN_PASSWORD=your-secure-admin-password-here

# ====== Email Notifications (for user approval/rejection) ======
# Email service to use: gmail, sendgrid, or custom SMTP
EMAIL_SERVICE=gmail

# Gmail configuration (if using EMAIL_SERVICE=gmail)
# For Gmail: use an "App Password" from your Google account
# https://support.google.com/accounts/answer/185833
EMAIL_USER=noreply@yourcompany.com
EMAIL_PASSWORD=your-app-specific-password

# Custom SMTP configuration (if not using gmail)
# SMTP_HOST=smtp.yourserver.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Email sender display name and dashboard URL
EMAIL_FROM=Analytics Dashboard <noreply@yourcompany.com>
DASHBOARD_URL=http://localhost:3000/analytics

# ====== Application Configuration ======
APP_NAME=Hemisphere Wellness

# ====== CORS Configuration (for production) ======
# Comma-separated list of allowed origins
# ALLOWED_ORIGINS=https://yourcompany.com,https://api.yourcompany.com
